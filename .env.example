# Server
PORT=3000
MONGODB_URI=mongodb+srv://<username>:<urlencoded-password>@<cluster-host>/<db-name>?retryWrites=true&w=majority
MONGODB_DB_NAME=gimbiya_mall
JWT_SECRET=replace-with-a-32-character-or-longer-secret
# Generate with: pnpm run jwt:secret

# Client (Vite)
VITE_APP_URL=http://localhost:3000
VITE_USE_BACKEND_AUTH=true
